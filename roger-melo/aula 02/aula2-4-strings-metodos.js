// métodos comuns de strings
const email = 'laracroft@rogermelo.com.br'

// const lastIndexOfA = email.lastIndexOf('a') // mostrar a posicao
// const emailSlice = email.slice(0, 4) // recortar na posicao
const emailReplace = email.replace('laracroft', 'chun-li') // substituir caractere

console.log(emailReplace)