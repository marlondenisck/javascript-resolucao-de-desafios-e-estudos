const numbers = new Array(1, 2, 3)
const obj = new Object()

console.log(numbers, obj)

// converter para boolean
Boolean(0) // false

// converter string para numero
Number('7') // 7


// Usar a palavra "new" instancia um novo objeto
// ao usar somente o construtor podes converter os dados

// Wrapper Objets
// https://developer.mozilla.org/en-US/docs/Glossary/Primitive#primitive_wrapper_objects_in_javascript