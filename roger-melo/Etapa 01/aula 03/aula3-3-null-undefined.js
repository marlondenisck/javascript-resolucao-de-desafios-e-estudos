let emptiness // undefined atribuido automaticamente

console.log(emptiness, emptiness + 3, `o valor de ${emptiness}`)

// quando vc quiser indicar INTENCIONALMENTE que a varial NÃO tem valor use null
let isNull = null

console.log(isNull, isNull + 3, `o valor de ${isNull}`)